# Python / Venv
__pycache__/
*.pyc
.venv/
venv/
.env

# Node
node_modules/
npm-debug.log

# OS
.DS_Store
Thumbs.db

# --- DADOS (A MUDANÇA IMPORTANTE) ---
# Ignora a pasta de dados brutos (CSVs da Susep, etc)
data/raw/

# Ignora snapshots históricos (para não inchar o repo)
data/snapshots/

# Mantém a estrutura de pastas (opcional, se tiver .gitkeep)
!data/raw/.gitkeep
!data/snapshots/.gitkeep

# Mantém os dados processados/agregados (opcional, mas útil para diffs)
!data/derived/
