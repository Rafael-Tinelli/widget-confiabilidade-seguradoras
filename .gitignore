# Python / Venv
__pycache__/
*.pyc
.venv/
venv/
.env

# Node
node_modules/
npm-debug.log
dist/
widget-ui/dist/

# Testes e Linters
.pytest_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# --- DADOS ---
# Ignora pastas de dados brutos e snapshots (gerados a cada run)
data/raw/
data/snapshots/

# Segurança extra: Ignora temporários do extrator onde quer que estejam
dump_latest.bin
cg_extract_*.csv
*.tmp

# Mantém a estrutura de pastas (gitkeep)
!data/raw/.gitkeep
!data/snapshots/.gitkeep

# IMPORTANTE: Mantém os dados processados (aggregated.json, insurers.json)
# Isso permite que o frontend consuma os dados direto do repo se necessário
!data/derived/
